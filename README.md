# 景区客流安全调度

本项目描述景区路段、换乘点、索道、天梯、接驳车辆和夜间活动的运行关系。闸机、设施、天气与匿名位置区段产生带观测时间的事件，迟到数据和失联传感器会降低占用估计的置信度。

路线包含方向、容量、体力等级和关闭窗口。临时管制记录发起人、原因、到期时间与解除人员。示例事件覆盖峰值入园、索道停运、暴雨告警、单向疏散和设施恢复。

`python3 service.py --check` 执行基础检查；`python3 service.py --port 8000` 启动健康端点。
